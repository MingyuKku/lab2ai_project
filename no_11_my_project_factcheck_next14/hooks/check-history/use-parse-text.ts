import { DocsBottomDetailsInfo } from "@/services/check-history/_types";
// import { ParseHistoryOriginText } from "./_types";

export const useParseText = () => {

    // const getOriginText = (docsOriginalText: string | null): ParseHistoryOriginText[] => {
    //     if (!docsOriginalText) return [];

    //     const parse = docsOriginalText.split('\n').map<ParseHistoryOriginText>((text => ({
    //         text: text,
    //     })));
        
    //     console.log('하하', parse)
    //     return parse;
    // }

    return {
        // createOriginText,
        // getOriginText,
    }
}